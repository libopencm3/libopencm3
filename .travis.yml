language: c
script:
 - make
 - make doc

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y doxygen

addons:
  apt:
    sources:
    - sourceline: 'ppa:team-gcc-arm-embedded/ppa'
    packages:
    - gcc-arm-embedded
